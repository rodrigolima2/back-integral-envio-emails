<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * {
            box-sizing: border-box;

            font-family: sans-serif;

            padding: 0;
            margin: 0;
        }

        body {
            display: flex;
            flex-direction: column;
            align-items: center;

            background-color: rgb(221, 221, 221);
        }

        .header {
            display: flex;
            align-items: center;
            justify-content: flex-start;

            width: 100%;

            background-color: blueviolet;

            padding: 50px;
            margin-bottom: 30px;
        }

        .header__title {
            font-size: larger;
            font-weight: bolder;
            color: #fff;
        }

        .card {
            display: flex;
            flex-direction: column;
            align-items: center;

            width: 80%;

            background-color: #fff;

            border-radius: 10px;

            padding: 50px;
        }

        .card__subtitle {
            border-bottom: 3px solid blueviolet;
            margin-bottom: 50px;
        }

        .card__text {
            color: #1a1a1a;
            line-height: 1.4;
            font-size: 19.5px;
            font-family: "Times", "Times New Roman", serif;
        }
    </style>
</head>

<body>
    <header class="header">
        <h1 class="header__title">Olá, {{nome}}</h1>
    </header>
    <div class="card">
        <h2 class="card__subtitle">Newsletter do dia</h2>
        <p class="card__text">{{mensagem}}</p>
    </div>
</body>

</html>